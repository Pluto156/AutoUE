# 进度

## 2026-06-27

- 创建任务 `autoue-puerts-workflow-adaptation`。

## 2026-06-28

- 撤销旧 Phase1 / Phase2 `done` 结论：旧结果只证明配置化、TS 旁路和一次真实 Codex CLI 静态切片，不证明完整迁移。
- 冻结新完成边界：本轮目标是 **Python 闭环完整**，不包含 UE Editor 内试玩。
- 重建 Phase0–Phase5 卡；当前进入 Phase1：原流程审计与迁移契约。
- 完成 Phase1：审计 `default.json` 11 节点旧链、`puerts_ts.json` 5 节点 TS 草案 + 3 个 disabled banned nodes，补全旧节点职责/副作用/prompt 残留表，定稿迁移覆盖表，并冻结 Phase2 执行契约。
- 当前切到 Phase2 准备：下一步按 `design/Phase2-PuerTS-Agent真实迁移.md` 实现真实迁移；Phase2 仍为 `pending`。

- 完成 Phase2：active workflow 迁移为 6 节点 PuerTS/TS 链路；新增 `TypeScriptInteractiveObjectGenerator`；scripted smoke 与真实 Codex LLM run `data/output-real-002/demo_1/` 均通过 Phase2 validator；当前切到 Phase3 准备。
- 用户确认范围决策 A：UE API MCP 查询链路纳入 Phase2 完成标准；Phase2 从 done 回退为 implementing，旧 6 节点模板化 run 保留为阶段性证据，下一步补 `ThinGameplayFlowPlanner`、`UEApiMCPFeasibilitySearcher`、`PuerTSRuntimeMappingPlanner`。
- 明确验证职责分界：Phase2 只生成可执行验证计划和静态 trace gate；Phase3 负责 PIEValidationRunner、runtime 执行器、结果收集和 pass/fail 判定。
- 完成 Phase2 最终迁移：active workflow 升级为 9 节点，补齐 `ThinGameplayFlowPlanner`、`UEApiMCPFeasibilitySearcher`、`PuerTSRuntimeMappingPlanner`；scripted smoke 与真实 Codex LLM run `data/output-real-008/demo_1/` 均通过 Phase2 validator，pytest/py_compile/conventions 通过；当前切到 Phase3，Phase3 仍为 `pending`。
- 新增 Phase3.5 规划：模型分层降级实验只作为 pending 弱档次表落档，当前 Phase2 提交仍使用已验证的 high/xhigh 配置。
- AutoUE Phase2 代码与证据已提交：`fefc69d feat: complete phase2 puerts workflow migration`。
- 回退 Phase3 done 标记：Python runtime harness 已完成，但只算 Phase3 子项；Phase3 重新回到 implementing，done 标准改为必须真实启动或连接 UE/PIE/PuerTS runtime 并跑通至少一条流程。
- 完成 Phase3：执行真实 AIDev 端到端流程，证据目录 `D:\UE5.7.4\AIDev\_codex_backups\20260628-181719-autoue-phase3-real`；AutoUE 9 active nodes 真跑、validator、tsc、TypeScript Blueprint、UE Editor/PIE、真实键盘输入移动/攻击/exit 全部通过；Phase3 标记为 done。
- 回退 Phase3 done：上一条真实 AIDev/PIE 证据包含主 agent 手写 `AutoUEPhase3Runtime.ts`、Character/GameMode Adapter、战斗和摄像机中间层；它只能证明手写集成可跑，不能证明 AutoUE 节点全自动完成。证据和回退说明见 `D:\UE5.7.4\AIDev\_codex_backups\20260628-181719-autoue-phase3-real\phase3-manual-bridge-retraction.md`。
- 追加 Phase3 重跑裁决：砍掉过度新增节点的方案 B，保留 9 个 active 节点并按 `game-proto-mapping` 最新 02/03/04/05/06/08 口径收紧；下一轮从 clean baseline 使用带冰冻陷阱、冻结状态、相机反馈、VFX、锁门出口的偏复杂单房间系统重跑。
- Phase3 复杂系统 clean rerun 已执行但 **不标 done**：9 个 AutoUE 节点、validator、AIDev tsc、TypeScript Blueprint、UE Editor/PIE、GameMode/Pawn 加载通过；但真实键盘输入没有进入 PIE PlayerController、生成冰冻陷阱存在重复冻结软锁、截图仍取 editor viewport 远景相机、standalone `-game` fallback 因 `GEditor` assertion 崩溃。详细证据见 `test/Phase3-complex-rerun-20260628.md`。

- Phase3 最终完成：在不新增 Combat/Camera/Adapter 专用 LLM 节点的前提下，修正 `TypeScriptCodeGenerator` 支撑模板；复杂系统从零重跑 9 active nodes，AIDev 清空导入、tsc、TypeScript Blueprint、UE Editor/PIE、application-layer input harness、冰冻陷阱/VFX/SideCamera/近战/出口闭环全部通过。证据见 `test/Phase3-complex-rerun-20260629.md`。
