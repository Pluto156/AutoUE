{
  "modules": [
    {
      "module_name": "ChestModule",
      "header_code": "#pragma once\n#include \"CoreMinimal.h\"\n#include \"IModuleBase.h\"\n\n/**\n * Chest interaction module\n * Manages chest state (locked/unlocked), opening animation, loot spawning,\n * and delegates item collection to InventoryModule.\n */\nclass LLMEDITOR_API FChestModule : public IModuleBase\n{\npublic:\n    virtual void Initialize() override\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[ChestModule] Initialized\"));\n    }\n\n    virtual void Shutdown() override\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[ChestModule] Shutdown\"));\n        OpenedChests.Empty();\n    }\n\n    static FName StaticModuleName() { return TEXT(\"ChestModule\"); }\n\npublic:\n    /** Attempt to open a chest: validate lock state, play animation, spawn loot, notify inventory */\n    void OpenChest(const FString& ChestID, bool bIsLocked);\n\n    /** Mark chest as opened (idempotent) */\n    void MarkChestAsOpened(const FString& ChestID);\n\n    /** Check if chest has already been opened */\n    bool IsChestOpened(const FString& ChestID) const;\n\n    /** Print current opened chest status */\n    void PrintChestStatus() const;\n\nprivate:\n    /** Track opened chests by ID */\n    TSet<FString> OpenedChests;\n};",
      "source_code": "#include \"ChestModule.h\"\n#include \"ModuleRegistry.h\"\n#include \"ModuleMacro.h\"\n#include \"MyModuleManager.h\"\n#include \"EvaluationLogModule.h\"\n#include \"CustomModules.h\"\nvoid FChestModule::OpenChest(const FString& ChestID, bool bIsLocked)\n{\n    auto& Manager = FMyModuleManager::Instance();\n    auto* EvalLog = Manager.GetModule<FEvaluationLogModule>();\n    auto* Inventory = Manager.GetModule<FInventoryModule>();\n\n    if (ChestID.IsEmpty())\n    {\n        UE_LOG(LogTemp, Warning, TEXT(\"[ChestModule] Invalid chest ID.\"));\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                TEXT(\"[ChestModule] OpenChest failed: invalid chest ID.\"));\n        }\n        return;\n    }\n\n    if (bIsLocked)\n    {\n        UE_LOG(LogTemp, Warning, TEXT(\"[ChestModule] Cannot open locked chest '%s'.\"), *ChestID);\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                FString::Printf(\n                    TEXT(\"[ChestModule] OpenChest denied: chest '%s' is locked.\"),\n                    *ChestID));\n        }\n        return;\n    }\n\n    if (IsChestOpened(ChestID))\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[ChestModule] Chest '%s' already opened.\"), *ChestID);\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                FString::Printf(\n                    TEXT(\"[ChestModule] OpenChest skipped: chest '%s' already opened.\"),\n                    *ChestID));\n        }\n        return;\n    }\n\n    // Simulate opening animation\n    UE_LOG(LogTemp, Log, TEXT(\"[ChestModule] Playing opening animation for chest '%s'.\"), *ChestID);\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(\n            FString::Printf(\n                TEXT(\"[ChestModule] Opening chest: '%s', animation started.\"),\n                *ChestID));\n    }\n\n    // Spawn loot (mock: fixed set)\n    TArray<FString> LootItems = { TEXT(\"HealthPotion\"), TEXT(\"RustyKey\"), TEXT(\"GoldCoin\") };\n\n    UE_LOG(LogTemp, Log, TEXT(\"[ChestModule] Spawning %d loot items from chest '%s'.\"), LootItems.Num(), *ChestID);\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(\n            FString::Printf(\n                TEXT(\"[ChestModule] Loot spawned from '%s': %d items.\"),\n                *ChestID,\n                LootItems.Num()));\n    }\n\n    // Delegate to InventoryModule\n    if (Inventory)\n    {\n        for (const FString& Item : LootItems)\n        {\n            Inventory->AddItem(Item);\n        }\n    }\n    else\n    {\n        UE_LOG(LogTemp, Error, TEXT(\"[ChestModule] InventoryModule not available — skipping item collection.\"));\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                TEXT(\"[ChestModule] InventoryModule unavailable; loot not added.\"));\n        }\n    }\n\n    MarkChestAsOpened(ChestID);\n    PrintChestStatus();\n}\n\nvoid FChestModule::MarkChestAsOpened(const FString& ChestID)\n{\n    if (!ChestID.IsEmpty())\n    {\n        OpenedChests.Add(ChestID);\n    }\n}\n\nbool FChestModule::IsChestOpened(const FString& ChestID) const\n{\n    return !ChestID.IsEmpty() && OpenedChests.Contains(ChestID);\n}\n\nvoid FChestModule::PrintChestStatus() const\n{\n    auto& Manager = FMyModuleManager::Instance();\n    auto* EvalLog = Manager.GetModule<FEvaluationLogModule>();\n\n    UE_LOG(LogTemp, Log, TEXT(\"=== Chest Status Report ===\"));\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(TEXT(\"[ChestModule] Chest status report begin.\"));\n    }\n\n    if (OpenedChests.Num() == 0)\n    {\n        UE_LOG(LogTemp, Log, TEXT(\" - No chests opened yet.\"));\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(TEXT(\"[ChestModule] No chests opened.\"));\n        }\n    }\n    else\n    {\n        for (const FString& ChestID : OpenedChests)\n        {\n            UE_LOG(LogTemp, Log, TEXT(\" - Opened: %s\"), *ChestID);\n\n            if (EvalLog)\n            {\n                EvalLog->WriteEvalLog(\n                    FString::Printf(\n                        TEXT(\"[ChestModule] Chest opened: %s.\"),\n                        *ChestID));\n            }\n        }\n    }\n\n    UE_LOG(LogTemp, Log, TEXT(\"==========================\"));\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(TEXT(\"[ChestModule] Chest status report end.\"));\n    }\n}\n\n/** Register module */\nREGISTER_MODULE(FChestModule)"
    },
    {
      "module_name": "CampfireModule",
      "header_code": "#pragma once\n#include \"CoreMinimal.h\"\n#include \"IModuleBase.h\"\n\n/**\n * Campfire interaction module\n * Manages campfire ignition state, visual feedback (flame activation),\n * and quest event triggering (e.g., marking quest step complete or spawning quest NPC).\n */\nclass LLMEDITOR_API FCampfireModule : public IModuleBase\n{\npublic:\n    virtual void Initialize() override\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[CampfireModule] Initialized\"));\n    }\n\n    virtual void Shutdown() override\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[CampfireModule] Shutdown\"));\n        IgnitedCampfires.Empty();\n    }\n\n    static FName StaticModuleName() { return TEXT(\"CampfireModule\"); }\n\npublic:\n    /** Toggle campfire ignition state: activate flame visuals and trigger associated quest event */\n    void InteractWithCampfire(const FString& CampfireID, const FString& QuestID);\n\n    /** Check if campfire is currently ignited */\n    bool IsCampfireIgnited(const FString& CampfireID) const;\n\n    /** Print current ignited campfire status */\n    void PrintCampfireStatus() const;\n\nprivate:\n    /** Track ignited campfires by ID */\n    TSet<FString> IgnitedCampfires;\n};",
      "source_code": "#include \"CampfireModule.h\"\n#include \"ModuleRegistry.h\"\n#include \"ModuleMacro.h\"\n#include \"MyModuleManager.h\"\n#include \"EvaluationLogModule.h\"\n#include \"CustomModules.h\"\nvoid FCampfireModule::InteractWithCampfire(const FString& CampfireID, const FString& QuestID)\n{\n    auto& Manager = FMyModuleManager::Instance();\n    auto* EvalLog = Manager.GetModule<FEvaluationLogModule>();\n    auto* Quest = Manager.GetModule<FQuestModule>();\n\n    if (CampfireID.IsEmpty())\n    {\n        UE_LOG(LogTemp, Warning, TEXT(\"[CampfireModule] Invalid campfire ID.\"));\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                TEXT(\"[CampfireModule] InteractWithCampfire failed: invalid campfire ID.\"));\n        }\n        return;\n    }\n\n    const bool bWasAlreadyIgnited = IsCampfireIgnited(CampfireID);\n\n    if (bWasAlreadyIgnited)\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[CampfireModule] Campfire '%s' already ignited.\"), *CampfireID);\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                FString::Printf(\n                    TEXT(\"[CampfireModule] InteractWithCampfire skipped: campfire '%s' already ignited.\"),\n                    *CampfireID));\n        }\n        return;\n    }\n\n    // Activate flame visuals (mock)\n    UE_LOG(LogTemp, Log, TEXT(\"[CampfireModule] Activating flame visuals for campfire '%s'.\"), *CampfireID);\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(\n            FString::Printf(\n                TEXT(\"[CampfireModule] Flame activated for campfire: '%s'.\"),\n                *CampfireID));\n    }\n\n    // Mark as ignited\n    IgnitedCampfires.Add(CampfireID);\n\n    // Trigger associated quest event if QuestModule is available\n    if (Quest && !QuestID.IsEmpty())\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[CampfireModule] Triggering quest event '%s' for campfire '%s'.\"), *QuestID, *CampfireID);\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                FString::Printf(\n                    TEXT(\"[CampfireModule] Quest event triggered: '%s' from campfire '%s'.\"),\n                    *QuestID,\n                    *CampfireID));\n        }\n\n        Quest->TriggerEvent(QuestID);\n    }\n    else if (!QuestID.IsEmpty())\n    {\n        UE_LOG(LogTemp, Error, TEXT(\"[CampfireModule] QuestModule unavailable — skipping quest event '%s'.\"), *QuestID);\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                FString::Printf(\n                    TEXT(\"[CampfireModule] QuestModule unavailable; event '%s' not triggered.\"),\n                    *QuestID));\n        }\n    }\n\n    PrintCampfireStatus();\n}\n\nbool FCampfireModule::IsCampfireIgnited(const FString& CampfireID) const\n{\n    return !CampfireID.IsEmpty() && IgnitedCampfires.Contains(CampfireID);\n}\n\nvoid FCampfireModule::PrintCampfireStatus() const\n{\n    auto& Manager = FMyModuleManager::Instance();\n    auto* EvalLog = Manager.GetModule<FEvaluationLogModule>();\n\n    UE_LOG(LogTemp, Log, TEXT(\"=== Campfire Status Report ===\"));\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(TEXT(\"[CampfireModule] Campfire status report begin.\"));\n    }\n\n    if (IgnitedCampfires.Num() == 0)\n    {\n        UE_LOG(LogTemp, Log, TEXT(\" - No campfires ignited yet.\"));\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(TEXT(\"[CampfireModule] No campfires ignited.\"));\n        }\n    }\n    else\n    {\n        for (const FString& CampfireID : IgnitedCampfires)\n        {\n            UE_LOG(LogTemp, Log, TEXT(\" - Ignited: %s\"), *CampfireID);\n\n            if (EvalLog)\n            {\n                EvalLog->WriteEvalLog(\n                    FString::Printf(\n                        TEXT(\"[CampfireModule] Campfire ignited: %s.\"),\n                        *CampfireID));\n            }\n        }\n    }\n\n    UE_LOG(LogTemp, Log, TEXT(\"==============================\"));\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(TEXT(\"[CampfireModule] Campfire status report end.\"));\n    }\n}\n\n/** Register module */\nREGISTER_MODULE(FCampfireModule)"
    },
    {
      "module_name": "PatrolAwarenessModule",
      "header_code": "#pragma once\n#include \"CoreMinimal.h\"\n#include \"IModuleBase.h\"\n\n/**\n * Patrol awareness module\n * Monitors player proximity to EnemyGuard patrol paths and emits contextual awareness signals\n * (e.g., guard pauses, turns, or enters alert state) — WITHOUT initiating combat.\n * Combat logic remains external (e.g., handled by BattleModule).\n * Designed to run continuously via tick or overlap events; not invoked from HandleInteraction().\n */\nclass LLMEDITOR_API FPatrolAwarenessModule : public IModuleBase\n{\npublic:\n    virtual void Initialize() override\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[PatrolAwarenessModule] Initialized\"));\n    }\n\n    virtual void Shutdown() override\n    {\n        UE_LOG(LogTemp, Log, TEXT(\"[PatrolAwarenessModule] Shutdown\"));\n        ActiveAlertZones.Empty();\n        TrackedGuards.Empty();\n    }\n\n    static FName StaticModuleName() { return TEXT(\"PatrolAwarenessModule\"); }\n\npublic:\n    /** Register a guard for proximity monitoring (e.g. on spawn or initialization) */\n    void RegisterGuard(const FString& GuardID, const FVector& PatrolStart, const FVector& PatrolEnd);\n\n    /** Unregister a guard (e.g. on destruction or despawn) */\n    void UnregisterGuard(const FString& GuardID);\n\n    /** Evaluate current awareness state for a guard based on player location */\n    void UpdateGuardAwareness(const FString& GuardID, const FVector& PlayerLocation, float AwarenessRadius = 500.0f);\n\n    /** Query whether guard is currently in alert state (non-combat, pre-combat cue) */\n    bool IsGuardAlerted(const FString& GuardID) const;\n\n    /** Print current tracked guards and alert status */\n    void PrintAwarenessStatus() const;\n\nprivate:\n    /** Track registered guards: ID → patrol path endpoints */\n    TMap<FString, TPair<FVector, FVector>> TrackedGuards;\n\n    /** Track guards currently in alert state (e.g., paused, turned toward player) */\n    TSet<FString> ActiveAlertZones;\n};",
      "source_code": "#include \"PatrolAwarenessModule.h\"\n#include \"ModuleRegistry.h\"\n#include \"ModuleMacro.h\"\n#include \"MyModuleManager.h\"\n#include \"EvaluationLogModule.h\"\n#include \"CustomModules.h\"\nvoid FPatrolAwarenessModule::RegisterGuard(const FString& GuardID, const FVector& PatrolStart, const FVector& PatrolEnd)\n{\n    auto& Manager = FMyModuleManager::Instance();\n    auto* EvalLog = Manager.GetModule<FEvaluationLogModule>();\n\n    if (GuardID.IsEmpty())\n    {\n        UE_LOG(LogTemp, Warning, TEXT(\"[PatrolAwarenessModule] Invalid guard ID.\"));\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                TEXT(\"[PatrolAwarenessModule] RegisterGuard failed: invalid guard ID.\"));\n        }\n        return;\n    }\n\n    TrackedGuards.Add(GuardID, TPair<FVector, FVector>(PatrolStart, PatrolEnd));\n\n    UE_LOG(LogTemp, Log,\n        TEXT(\"[PatrolAwarenessModule] Registered guard '%s' with patrol path (%s → %s).\"),\n        *GuardID,\n        *PatrolStart.ToString(),\n        *PatrolEnd.ToString());\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(\n            FString::Printf(\n                TEXT(\"[PatrolAwarenessModule] Guard registered: '%s'.\"),\n                *GuardID));\n    }\n}\n\nvoid FPatrolAwarenessModule::UnregisterGuard(const FString& GuardID)\n{\n    if (!GuardID.IsEmpty())\n    {\n        const bool bRemoved = TrackedGuards.Remove(GuardID) > 0;\n        if (bRemoved)\n        {\n            ActiveAlertZones.Remove(GuardID);\n\n            auto& Manager = FMyModuleManager::Instance();\n            auto* EvalLog = Manager.GetModule<FEvaluationLogModule>();\n\n            UE_LOG(LogTemp, Log, TEXT(\"[PatrolAwarenessModule] Unregistered guard '%s'.\"), *GuardID);\n\n            if (EvalLog)\n            {\n                EvalLog->WriteEvalLog(\n                    FString::Printf(\n                        TEXT(\"[PatrolAwarenessModule] Guard unregistered: '%s'.\"),\n                        *GuardID));\n            }\n        }\n    }\n}\n\nvoid FPatrolAwarenessModule::UpdateGuardAwareness(const FString& GuardID, const FVector& PlayerLocation, float AwarenessRadius)\n{\n    auto& Manager = FMyModuleManager::Instance();\n    auto* EvalLog = Manager.GetModule<FEvaluationLogModule>();\n\n    if (GuardID.IsEmpty())\n    {\n        UE_LOG(LogTemp, Warning, TEXT(\"[PatrolAwarenessModule] Invalid guard ID for awareness update.\"));\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                TEXT(\"[PatrolAwarenessModule] UpdateGuardAwareness failed: invalid guard ID.\"));\n        }\n        return;\n    }\n\n    const TPair<FVector, FVector>* PatrolPath = TrackedGuards.Find(GuardID);\n    if (!PatrolPath)\n    {\n        UE_LOG(LogTemp, Warning, TEXT(\"[PatrolAwarenessModule] Guard '%s' not registered — skipping awareness update.\"), *GuardID);\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                FString::Printf(\n                    TEXT(\"[PatrolAwarenessModule] Guard not tracked: '%s'.\"),\n                    *GuardID));\n        }\n        return;\n    }\n\n    // Compute closest point on patrol segment to player\n    const FVector SegmentDir = PatrolPath->Value - PatrolPath->Key;\n    const float SegmentLengthSq = SegmentDir.SizeSquared();\n    if (SegmentLengthSq < KINDA_SMALL_NUMBER)\n    {\n        UE_LOG(LogTemp, Warning, TEXT(\"[PatrolAwarenessModule] Degenerate patrol path for guard '%s'.\"), *GuardID);\n        return;\n    }\n\n    const FVector ToPlayer = PlayerLocation - PatrolPath->Key;\n    const float Projection = FVector::DotProduct(ToPlayer, SegmentDir) / SegmentLengthSq;\n    const FVector ClosestPoint = PatrolPath->Key + FMath::Clamp(Projection, 0.0f, 1.0f) * SegmentDir;\n    const float DistanceToClosest = (PlayerLocation - ClosestPoint).Size();\n\n    const bool bIsWithinRadius = DistanceToClosest <= AwarenessRadius;\n    const bool bWasAlerted = IsGuardAlerted(GuardID);\n\n    if (bIsWithinRadius && !bWasAlerted)\n    {\n        // Enter alert state: pause patrol, turn toward player, emit cue\n        ActiveAlertZones.Add(GuardID);\n\n        UE_LOG(LogTemp, Log,\n            TEXT(\"[PatrolAwarenessModule] Guard '%s' entered alert zone (dist=%.1f).\"),\n            *GuardID, DistanceToClosest);\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                FString::Printf(\n                    TEXT(\"[PatrolAwarenessModule] Guard alerted: '%s', distance=%.1f.\"),\n                    *GuardID,\n                    DistanceToClosest));\n        }\n    }\n    else if (!bIsWithinRadius && bWasAlerted)\n    {\n        // Exit alert state\n        ActiveAlertZones.Remove(GuardID);\n\n        UE_LOG(LogTemp, Log,\n            TEXT(\"[PatrolAwarenessModule] Guard '%s' exited alert zone (dist=%.1f).\"),\n            *GuardID, DistanceToClosest);\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(\n                FString::Printf(\n                    TEXT(\"[PatrolAwarenessModule] Guard de-alerted: '%s'.\"),\n                    *GuardID));\n        }\n    }\n}\n\nbool FPatrolAwarenessModule::IsGuardAlerted(const FString& GuardID) const\n{\n    return !GuardID.IsEmpty() && ActiveAlertZones.Contains(GuardID);\n}\n\nvoid FPatrolAwarenessModule::PrintAwarenessStatus() const\n{\n    auto& Manager = FMyModuleManager::Instance();\n    auto* EvalLog = Manager.GetModule<FEvaluationLogModule>();\n\n    UE_LOG(LogTemp, Log, TEXT(\"=== Patrol Awareness Status Report ===\"));\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(TEXT(\"[PatrolAwarenessModule] Awareness status report begin.\"));\n    }\n\n    if (TrackedGuards.Num() == 0)\n    {\n        UE_LOG(LogTemp, Log, TEXT(\" - No guards registered.\"));\n\n        if (EvalLog)\n        {\n            EvalLog->WriteEvalLog(TEXT(\"[PatrolAwarenessModule] No guards tracked.\"));\n        }\n    }\n    else\n    {\n        for (const auto& Pair : TrackedGuards)\n        {\n            const bool bIsAlerted = ActiveAlertZones.Contains(Pair.Key);\n            UE_LOG(LogTemp, Log,\n                TEXT(\" - %s: %s (path: %s → %s)\"),\n                *Pair.Key,\n                bIsAlerted ? TEXT(\"ALERTED\") : TEXT(\"idle\"),\n                *Pair.Value.Key.ToString(),\n                *Pair.Value.Value.ToString());\n\n            if (EvalLog)\n            {\n                EvalLog->WriteEvalLog(\n                    FString::Printf(\n                        TEXT(\"[PatrolAwarenessModule] Guard '%s': %s.\"),\n                        *Pair.Key,\n                        bIsAlerted ? TEXT(\"alerted\") : TEXT(\"idle\")));\n            }\n        }\n    }\n\n    UE_LOG(LogTemp, Log, TEXT(\"=====================================\"));\n\n    if (EvalLog)\n    {\n        EvalLog->WriteEvalLog(TEXT(\"[PatrolAwarenessModule] Awareness status report end.\"));\n    }\n}\n\n/** Register module */\nREGISTER_MODULE(FPatrolAwarenessModule)"
    }
  ]
}